<Window x:Class="MultiPingMonitor.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:classes="clr-namespace:MultiPingMonitor.Classes"
        xmlns:c="clr-namespace:MultiPingMonitor.Controls"
        xmlns:resource="clr-namespace:MultiPingMonitor.Properties"
        Title="MultiPingMonitor"
        WindowStartupLocation="Manual"
        WindowStyle="None"
        UseLayoutRounding="False"
        SnapsToDevicePixels="True"
        Height="400"
        Width="800"
        MinHeight="200"
        MinWidth="350"
        Background="{DynamicResource Theme.Background}"
        Loaded="Window_Loaded"
        StateChanged="Window_StateChanged"
        Closing="Window_Closing"
        ContentRendered="Window_ContentRendered"
        IsVisibleChanged="Window_IsVisibleChanged">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32"
                      ResizeBorderThickness="{x:Static SystemParameters.WindowResizeBorderThickness}"
                      GlassFrameThickness="0"
                      CornerRadius="0" />
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <!-- Override default context menu animation: set to no animation -->
        <PopupAnimation x:Key="{x:Static SystemParameters.MenuPopupAnimationKey}">None</PopupAnimation>

        <!-- Template for hostname textbox -->
        <ControlTemplate x:Key="TextBoxControlTemplate" TargetType="{x:Type TextBox}">
            <Grid>
                <Border x:Name="border"
                        CornerRadius="0"
                        SnapsToDevicePixels="True"
                        Background="{TemplateBinding Background}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        BorderBrush="{TemplateBinding BorderBrush}">
                    <ScrollViewer x:Name="PART_ContentHost"/>
                </Border>
                <TextBlock x:Name="placeholderText"
                           Text="{x:Static resource:Strings.MainWindow_EnterHostname}"
                           Visibility="Hidden"
                           FontSize="12"
                           Opacity="0.3"
                           HorizontalAlignment="Left"
                           VerticalAlignment="Center"
                           Padding="5,0,0,0"
                           IsHitTestVisible="False"/>
            </Grid>
            <ControlTemplate.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Secondary}"/>
                </Trigger>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="Text" Value=""/>
                        <Condition Property="IsKeyboardFocused" Value="True"/>
                    </MultiTrigger.Conditions>
                    <MultiTrigger.Setters>
                        <Setter Property="Visibility" TargetName="placeholderText" Value="Visible"/>
                    </MultiTrigger.Setters>
                </MultiTrigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <!-- Compact View: single-row item template for monitoring overview -->
        <DataTemplate x:Key="CompactProbeTemplate">
            <Border Style="{DynamicResource Style.CompactRow}"
                    Background="{Binding Status, Converter={StaticResource ProbeStatusToBackgroundBrushConverter}}"
                    MouseLeftButtonDown="CompactItem_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Status indicator -->
                    <TextBlock Grid.Column="0"
                               Text="{Binding Status, Converter={StaticResource ProbeStatusToCompactIndicatorConverter}}"
                               FontSize="12"
                               VerticalAlignment="Center"
                               Foreground="{Binding Status, Converter={StaticResource ProbeStatusToForegroundBrushConverter}}"
                               Margin="0,0,4,0"/>

                    <!-- Alias or Hostname label -->
                    <TextBlock Grid.Column="1"
                               FontSize="12"
                               FontWeight="SemiBold"
                               VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis"
                               Foreground="{Binding Status, Converter={StaticResource ProbeStatusToAliasBrushConverter}}">
                        <TextBlock.Text>
                            <MultiBinding Converter="{StaticResource AliasOrHostnameConverter}">
                                <Binding Path="Alias" UpdateSourceTrigger="PropertyChanged"/>
                                <Binding Path="Hostname" UpdateSourceTrigger="PropertyChanged"/>
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>

                    <!-- Latency value -->
                    <Border Grid.Column="2" Style="{DynamicResource Style.CompactLatencyBadge}">
                        <TextBlock Text="{Binding LastRoundtripTime, Converter={StaticResource RoundtripTimeToStringConverter}}"
                                   FontSize="11"
                                   FontFamily="Consolas"
                                   VerticalAlignment="Center"
                                   Foreground="{Binding Status, Converter={StaticResource ProbeStatusToStatisticsBrushConverter}}"
                                   MinWidth="32"
                                   TextAlignment="Right"/>
                    </Border>
                </Grid>
            </Border>
        </DataTemplate>

        <!-- Compact View: ItemsControl template with slim scrollable region -->
        <ControlTemplate x:Key="CompactItemsControlTemplate" TargetType="{x:Type ItemsControl}">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Focusable="False">
                <ScrollViewer.Resources>
                    <Style TargetType="{x:Type ScrollBar}" BasedOn="{StaticResource ScrollBarStyle.Compact}"/>
                </ScrollViewer.Resources>
                <ItemsPresenter/>
            </ScrollViewer>
        </ControlTemplate>
    </Window.Resources>

    <!-- Root Border with window border -->
    <Border Style="{DynamicResource Style.WindowMainPanel}">
    <!-- Root DockPanel -->
    <DockPanel Background="{DynamicResource Theme.Background}">

        <!-- Custom title bar -->
        <Grid x:Name="TitleBar" DockPanel.Dock="Top" Height="32" Background="{DynamicResource Theme.Surface}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <!-- App icon -->
            <Image Grid.Column="0"
                   Width="16" Height="16"
                   Margin="8,0,4,0"
                   Source="{StaticResource icon.vmping-logo-simple}" />
            <!-- Window title -->
            <TextBlock Grid.Column="1"
                       Style="{DynamicResource Style.TitleBar.Title}"
                       Text="{Binding Path=Title, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" />
            <!-- Pin / always-on-top toggle (Normal mode) -->
            <ToggleButton Grid.Column="2"
                          x:Name="NormalPinButton"
                          Click="PinButton_Click"
                          Style="{DynamicResource Style.PinToggleButton}"
                          WindowChrome.IsHitTestVisibleInChrome="True"
                          ToolTip="{x:Static resource:Strings.Options_AlwaysOnTop}"
                          ToolTipService.InitialShowDelay="800"
                          Width="32" MinHeight="32"
                          Cursor="Hand">
                <Path x:Name="NormalPinIcon"
                      Width="10" Height="12" Stretch="Uniform"
                      Fill="{DynamicResource Theme.Text.Secondary}"
                      VerticalAlignment="Center"
                      Data="M7,0 L9,0 9,4 12,6 12,8 8,8 8,14 7,14 7,8 3,8 3,6 7,4 Z" />
            </ToggleButton>
            <!-- Minimize button -->
            <Button Grid.Column="3"
                    Click="OnMinimizeButtonClick"
                    RenderOptions.EdgeMode="Aliased"
                    Style="{DynamicResource Style.TitleBarButton}"
                    AutomationProperties.Name="Minimize"
                    ToolTip="Minimize" ToolTipService.InitialShowDelay="1000">
                <Path Width="46" Height="32"
                      Data="M 18,15 H 28"
                      Stroke="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}"
                      StrokeThickness="1" />
            </Button>
            <!-- Maximize button -->
            <Button x:Name="maximizeButton"
                    Grid.Column="4"
                    Click="OnMaximizeRestoreButtonClick"
                    Style="{DynamicResource Style.TitleBarButton}"
                    AutomationProperties.Name="Maximize"
                    ToolTip="Maximize" ToolTipService.InitialShowDelay="1000">
                <Path Width="46" Height="32"
                      Data="M 18.5,10.5 H 27.5 V 19.5 H 18.5 Z"
                      Stroke="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}"
                      StrokeThickness="1" />
            </Button>
            <!-- Restore button -->
            <Button x:Name="restoreButton"
                    Grid.Column="4"
                    Click="OnMaximizeRestoreButtonClick"
                    Style="{DynamicResource Style.TitleBarButton}"
                    Visibility="Collapsed"
                    AutomationProperties.Name="Restore Down"
                    ToolTip="Restore Down" ToolTipService.InitialShowDelay="1000">
                <Path Width="46" Height="32"
                      Data="M 18.5,12.5 H 25.5 V 19.5 H 18.5 Z M 20.5,12.5 V 10.5 H 27.5 V 17.5 H 25.5"
                      Stroke="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}"
                      StrokeThickness="1" />
            </Button>
            <!-- Close button -->
            <Button Grid.Column="5"
                    Click="OnCloseButtonClick"
                    Style="{DynamicResource Style.TitleBarCloseButton}"
                    AutomationProperties.Name="Close"
                    ToolTip="Close" ToolTipService.InitialShowDelay="1000">
                <Path Width="46" Height="32"
                      Data="M 18,11 27,20 M 18,20 27,11"
                      Stroke="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}"
                      StrokeThickness="1" />
            </Button>
        </Grid>

        <!-- Separator line between title bar and menu -->
        <Border x:Name="TitleBarSeparator" DockPanel.Dock="Top" Style="{DynamicResource Style.TitleBarSeparator}" />

        <!-- Menu bar -->
        <Menu x:Name="MainMenu" DockPanel.Dock="Top"
              FontStretch="Normal"
              Background="{DynamicResource Theme.Surface}"
              Padding="4,2">
            <Menu.Resources>
                <Style TargetType="{x:Type MenuItem}" BasedOn="{StaticResource MenuItemStyle}"/>
            </Menu.Resources>
            <Menu.ItemsPanel>
                <ItemsPanelTemplate>
                    <DockPanel HorizontalAlignment="Stretch" LastChildFill="False" Background="{DynamicResource Theme.Surface}"/>
                </ItemsPanelTemplate>
            </Menu.ItemsPanel>
            
            <!-- Menu: Add Probe -->
            <MenuItem x:Name="AddProbeMenu">
                <MenuItem.Header>
                    <AccessText Text="{x:Static resource:Strings.Toolbar_Add}" VerticalAlignment="Center"/>
                </MenuItem.Header>
                <MenuItem.Icon>
                    <Path Data="{StaticResource geom.menu.add}" Fill="{DynamicResource Theme.Text.Primary}"
                          Width="14" Height="14" Stretch="Uniform" VerticalAlignment="Center"/>
                </MenuItem.Icon>
            </MenuItem>

            <!-- Menu: Columns -->
            <MenuItem>
                <MenuItem.Header>
                    <AccessText Text="{x:Static resource:Strings.Toolbar_Columns}" VerticalAlignment="Center"/>
                </MenuItem.Header>
                <MenuItem.Icon>
                    <Path Data="{StaticResource geom.menu.columns-grid}" Fill="{DynamicResource Theme.Text.Primary}"
                          Width="14" Height="14" Stretch="Uniform" VerticalAlignment="Center"/>
                </MenuItem.Icon>
                <!-- Columns slider -->
                <StackPanel Orientation="Horizontal">
                    <Label Content="{Binding ElementName=ColumnCount, Path=Value}"
                           Margin="0,0,15,0"
                           Width="30"
                           FontSize="14"
                           HorizontalContentAlignment="Center"
                           VerticalContentAlignment="Center"/>
                    <Slider Name="ColumnCount"
                            Style="{DynamicResource SliderStyle}"
                            ValueChanged="ColumnCount_ValueChanged"
                            Minimum="1"
                            Maximum="10"
                            Value="3"
                            TickFrequency="1"
                            SmallChange="1"
                            TickPlacement="BottomRight"
                            IsSnapToTickEnabled="True"
                            Width="200"
                            Foreground="{DynamicResource Theme.Text.Primary}"
                            Margin="0,8"/>
                </StackPanel>
            </MenuItem>

            <!-- Menu: Start/Stop All -->
            <MenuItem x:Name="StartStopMenu">
                <MenuItem.Header>
                    <AccessText Name="StartStopMenuHeader"
                                VerticalAlignment="Center"
                                Text="{Binding Path=(classes:Probe.ActiveCount), Converter={StaticResource ProbeCountToGlobalStartStopText}}"/>
                </MenuItem.Header>
                <MenuItem.Icon>
                    <Path Data="{Binding Path=(classes:Probe.ActiveCount), Converter={StaticResource ProbeCountToGlobalStartStopIcon}}"
                          Fill="{DynamicResource Theme.Text.Primary}"
                          Width="14" Height="14" Stretch="Uniform" VerticalAlignment="Center"/>
                </MenuItem.Icon>
            </MenuItem>

            <!-- Menu: Main menu dropdown -->
            <MenuItem Width="40"
                      DockPanel.Dock="Right"
                      Template="{StaticResource TopLevelHeaderNoPaddingTemplate}">
                <MenuItem.Icon>
                    <Path Data="{StaticResource geom.menu.chevron-down}" Fill="{DynamicResource Theme.Text.Primary}"
                          Width="14" Height="14" Stretch="Uniform" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </MenuItem.Icon>

                <!-- Menu: Options -->
                <MenuItem Name="OptionsMenu"
                          Header="{x:Static resource:Strings.Menu_Options}"
                          InputGestureText="F10">
                    <MenuItem.Icon>
                        <Path Data="{StaticResource geom.menu.options}" Fill="{DynamicResource Theme.Text.Primary}"
                              Width="14" Height="14" Stretch="Uniform" VerticalAlignment="Center"/>
                    </MenuItem.Icon>
                </MenuItem>
                <!-- Menu: Switch display mode (Normal ↔ Compact) -->
                <MenuItem x:Name="ToggleDisplayModeMenu" Click="ToggleDisplayMode_Click">
                    <MenuItem.Icon>
                        <Path Data="{StaticResource geom.menu.toggle-display}" Fill="{DynamicResource Theme.Text.Primary}"
                              Width="14" Height="14" Stretch="Uniform" VerticalAlignment="Center"/>
                    </MenuItem.Icon>
                </MenuItem>
                <!-- Menu: Compact targets submenu -->
                <MenuItem Header="{x:Static resource:Strings.Menu_CompactTargets}" x:Name="CompactTargetsMenu"
                          SubmenuOpened="CompactTargetsMenu_SubmenuOpened">
                    <MenuItem.Icon>
                        <Path Data="{StaticResource geom.menu.compact-view}" Fill="{DynamicResource Theme.Text.Primary}"
                              Width="14" Height="14" Stretch="Uniform" VerticalAlignment="Center"/>
                    </MenuItem.Icon>
                    <MenuItem x:Name="MenuCompactSourceNormal"
                              Header="{x:Static resource:Strings.Options_CompactSource_NormalTargets}"
                              IsCheckable="True"
                              Click="MenuCompactSourceNormal_Click"/>
                    <MenuItem x:Name="MenuCompactSourceCustom"
                              Header="{x:Static resource:Strings.Options_CompactSource_CustomTargets}"
                              IsCheckable="True"
                              Click="MenuCompactSourceCustom_Click"/>
                    <!-- Dynamic compact set items and Manage Sets are added in code-behind -->
                </MenuItem>
                <Separator />
                <!-- Menu: Status history -->
                <MenuItem Name="StatusHistoryMenu" Header="{x:Static resource:Strings.Menu_StatusHistory}" InputGestureText="F12">
                    <MenuItem.Icon>
                        <Path Data="{StaticResource geom.menu.status-history}" Fill="{DynamicResource Theme.Text.Primary}"
                              Width="14" Height="14" Stretch="Uniform" VerticalAlignment="Center"/>
                    </MenuItem.Icon>
                </MenuItem>
                <!-- Menu: Input addresses-->
                <MenuItem Name="MultiInputMenu" Header="{x:Static resource:Strings.Menu_InputAddresses}" InputGestureText="F2">
                    <MenuItem.Icon>
                        <Path Data="{StaticResource geom.menu.input-addresses}" Fill="{DynamicResource Theme.Text.Primary}"
                              Width="14" Height="14" Stretch="Uniform" VerticalAlignment="Center"/>
                    </MenuItem.Icon>
                </MenuItem>
                <!-- Menu: Popup alerts -->
                <MenuItem Header="{x:Static resource:Strings.Menu_PopupAlerts}">
                    <MenuItem Name="PopupNever"
                              Header="{x:Static resource:Strings.Notification_Never}"
                              Click="PopupNever_Click"
                              IsCheckable="True"/>
                    <MenuItem Name="PopupWhenMinimized"
                              Header="{x:Static resource:Strings.Notification_WhenMinimized}"
                              Click="PopupWhenMinimized_Click"
                              IsCheckable="True"/>
                    <MenuItem Name="PopupAlways"
                              Header="{x:Static resource:Strings.Notification_Always}"
                              Click="PopupAlways_Click"
                              IsCheckable="True"/>
                </MenuItem>
                <Separator />
                <!-- Menu: Favorite sets-->
                <MenuItem Header="{x:Static resource:Strings.Menu_FavoriteSets}" x:Name="FavoritesMenu">
                    <MenuItem Header="{x:Static resource:Strings.Menu_SaveToFavorites}" Click="CreateFavorite_Click">
                        <MenuItem.Icon>
                            <Path Data="{StaticResource geom.menu.new-favorite}" Fill="{DynamicResource Theme.Text.Primary}"
                                  Width="14" Height="14" Stretch="Uniform" VerticalAlignment="Center"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="{x:Static resource:Strings.Menu_ManageFavorites}" Click="ManageFavorites_Click">
                        <MenuItem.Icon>
                            <Path Data="{StaticResource geom.menu.favorite}" Fill="{DynamicResource Theme.Text.Primary}"
                                  Width="14" Height="14" Stretch="Uniform" VerticalAlignment="Center"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <Separator />
                </MenuItem>
                <!-- Menu: Aliases-->
                <MenuItem Header="{x:Static resource:Strings.Menu_Aliases}" x:Name="AliasesMenu">
                    <MenuItem Header="{x:Static resource:Strings.Menu_ManageAliases}" Click="ManageAliases_Click">
                        <MenuItem.Icon>
                            <Path Data="{StaticResource geom.menu.alias}" Fill="{DynamicResource Theme.Text.Primary}"
                                  Width="14" Height="14" Stretch="Uniform" VerticalAlignment="Center"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <Separator />
                </MenuItem>
                <Separator />
                <!-- Menu: Traceroute -->
                <MenuItem Header="{x:Static resource:Strings.Menu_Traceroute}" x:Name="TracerouteMenu" InputGestureText="Ctrl-T">
                    <MenuItem.Icon>
                        <Path Data="{StaticResource geom.menu.traceroute}" Fill="{DynamicResource Theme.Text.Primary}"
                              Width="14" Height="14" Stretch="Uniform" VerticalAlignment="Center"/>
                    </MenuItem.Icon>
                </MenuItem>
                <!-- Menu: Flood host -->
                <MenuItem Header="{x:Static resource:Strings.Menu_FloodHost}" x:Name="FloodHostMenu" InputGestureText="Ctrl-F">
                    <MenuItem.Icon>
                        <Path Data="{StaticResource geom.menu.flood-host}" Fill="{DynamicResource Theme.Text.Primary}"
                              Width="14" Height="14" Stretch="Uniform" VerticalAlignment="Center"/>
                    </MenuItem.Icon>
                </MenuItem>
                <!-- Menu: New Live Ping (manual/direct) -->
                <MenuItem Header="{x:Static resource:Strings.Menu_NewLivePing}" x:Name="NewLivePingMenu"
                          Click="NewLivePingMenu_Click">
                    <MenuItem.Icon>
                        <Path Data="{StaticResource geom.menu.new-live-ping}" Fill="{DynamicResource Theme.Text.Primary}"
                              Width="14" Height="14" Stretch="Uniform" VerticalAlignment="Center"/>
                    </MenuItem.Icon>
                </MenuItem>
                <!-- Menu: New instance -->
                <MenuItem Header="{x:Static resource:Strings.Menu_NewInstance}" x:Name="NewInstanceMenu" InputGestureText="Ctrl-N">
                    <MenuItem.Icon>
                        <Path Data="{StaticResource geom.menu.new-instance}" Fill="{DynamicResource Theme.Text.Primary}"
                              Width="14" Height="14" Stretch="Uniform" VerticalAlignment="Center"/>
                    </MenuItem.Icon>
                </MenuItem>
                <Separator />
                <!-- Menu: Help -->
                <MenuItem Header="{x:Static resource:Strings.Menu_Help}" x:Name="HelpMenu" InputGestureText="F1">
                    <MenuItem.Icon>
                        <Path Data="{StaticResource geom.menu.help}" Fill="{DynamicResource Theme.Text.Primary}"
                              Width="14" Height="14" Stretch="Uniform" VerticalAlignment="Center"/>
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
        </Menu>


        <!-- Compact mode: slim drag bar (hidden by default, shown in Compact mode) -->
        <Grid x:Name="CompactTitleBar" DockPanel.Dock="Top" Height="26" Background="{DynamicResource Theme.Surface}"
              Visibility="Collapsed" MouseLeftButtonDown="CompactTitleBar_MouseLeftButtonDown">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0"
                       Text="MultiPingMonitor"
                       FontSize="11"
                       Foreground="{DynamicResource Theme.Text.Secondary}"
                       VerticalAlignment="Center"
                       Margin="6,0,0,0"
                       TextTrimming="CharacterEllipsis"
                       Opacity="0.7"/>
            <!-- Compact title bar: pin / always-on-top toggle -->
            <ToggleButton Grid.Column="1"
                          x:Name="CompactPinButton"
                          Click="PinButton_Click"
                          Style="{DynamicResource Style.PinToggleButton}"
                          WindowChrome.IsHitTestVisibleInChrome="True"
                          ToolTip="{x:Static resource:Strings.Options_AlwaysOnTop}"
                          ToolTipService.InitialShowDelay="800"
                          Width="26" MinHeight="26"
                          Cursor="Hand">
                <Path x:Name="CompactPinIcon"
                      Width="9" Height="11" Stretch="Uniform"
                      Fill="{DynamicResource Theme.Text.Secondary}"
                      VerticalAlignment="Center"
                      Data="M7,0 L9,0 9,4 12,6 12,8 8,8 8,14 7,14 7,8 3,8 3,6 7,4 Z" />
            </ToggleButton>
            <!-- Compact title bar: menu button for quick access to compact data source -->
            <Button Grid.Column="2"
                    x:Name="CompactMenuButton"
                    Click="CompactMenuButton_Click"
                    Style="{DynamicResource Style.TitleBarButton}"
                    WindowChrome.IsHitTestVisibleInChrome="True"
                    ToolTip="{x:Static resource:Strings.Menu_CompactTargets}"
                    ToolTipService.InitialShowDelay="1000"
                    Width="36" MinHeight="26"
                    Cursor="Hand">
                <Path Data="M 3,5 L 12,5 M 3,9 L 12,9 M 3,13 L 12,13"
                      Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}"
                      StrokeThickness="1.2"
                      Width="15" Height="18"
                      Stretch="None"/>
            </Button>
            <Button Grid.Column="3"
                    Click="OnCloseButtonClick"
                    Style="{DynamicResource Style.TitleBarCloseButton}"
                    AutomationProperties.Name="Close"
                    ToolTip="Close" ToolTipService.InitialShowDelay="1000"
                    Width="36" MinHeight="26">
                <Path Width="10" Height="10"
                      Data="M0,0 L10,10 M0,10 L10,0"
                      Stretch="None"
                      Stroke="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}"
                      StrokeThickness="1" />
            </Button>
        </Grid>

        <!-- Compact mode: active set toolbar — shown below title bar when a custom set is active -->
        <Border x:Name="CompactSetToolbar"
                DockPanel.Dock="Top"
                Height="26"
                Visibility="Collapsed"
                Style="{DynamicResource Style.CompactSetToolbar}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <!-- Active compact set name -->
                <TextBlock x:Name="CompactSetNameText"
                           Grid.Column="0"
                           FontSize="11"
                           FontWeight="SemiBold"
                           TextTrimming="CharacterEllipsis"
                           Foreground="{DynamicResource Theme.Text.Primary}"
                           VerticalAlignment="Center"
                           Margin="2,0,4,0"/>
                <!-- Stopped badge: visible when the active compact set is stopped -->
                <Border x:Name="CompactStoppedBadge"
                        Grid.Column="1"
                        Visibility="Collapsed"
                        Background="{DynamicResource Theme.Warning}"
                        CornerRadius="{DynamicResource Style.CornerRadius.Small}"
                        Padding="4,1"
                        Margin="0,0,4,0"
                        VerticalAlignment="Center">
                    <TextBlock Text="{x:Static resource:Strings.Compact_SetStopped}"
                               FontSize="10"
                               FontWeight="Bold"
                               Foreground="{DynamicResource Theme.AccentForeground}"/>
                </Border>
                <!-- Add host quick-access button -->
                <Button Grid.Column="2"
                        x:Name="CompactAddHostButton"
                        Click="CompactAddHostButton_Click"
                        Style="{DynamicResource Style.TitleBarButton}"
                        WindowChrome.IsHitTestVisibleInChrome="True"
                        ToolTip="{x:Static resource:Strings.Compact_AddHost}"
                        ToolTipService.InitialShowDelay="800"
                        Width="28" MinHeight="26"
                        Cursor="Hand">
                    <Path Width="9" Height="9" Stretch="Uniform"
                          Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}"
                          VerticalAlignment="Center"
                          Data="{StaticResource geom.menu.add}"/>
                </Button>
                <!-- Remove host quick-access button -->
                <Button Grid.Column="3"
                        x:Name="CompactRemoveHostButton"
                        Click="CompactRemoveHostButton_Click"
                        Style="{DynamicResource Style.TitleBarButton}"
                        WindowChrome.IsHitTestVisibleInChrome="True"
                        ToolTip="{x:Static resource:Strings.Compact_RemoveHost}"
                        ToolTipService.InitialShowDelay="800"
                        Width="28" MinHeight="26"
                        Cursor="Hand">
                    <Path Width="9" Height="2" Stretch="Uniform"
                          Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}"
                          VerticalAlignment="Center"
                          Data="{StaticResource geom.menu.remove}"/>
                </Button>
                <!-- Start/Stop quick-access button -->
                <Button Grid.Column="4"
                        x:Name="CompactStartStopButton"
                        Click="CompactStartStopButton_Click"
                        Style="{DynamicResource Style.TitleBarButton}"
                        WindowChrome.IsHitTestVisibleInChrome="True"
                        ToolTipService.InitialShowDelay="800"
                        Width="28" MinHeight="26"
                        Cursor="Hand">
                    <Path x:Name="CompactStartStopIcon"
                          Width="8" Height="9" Stretch="Uniform"
                          Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}"
                          VerticalAlignment="Center"
                          Data="{StaticResource geom.menu.start}"/>
                </Button>
            </Grid>
        </Border>

        <!-- Items Control -->
        <ItemsControl Name="ProbeItemsControl" IsTabStop="False" Focusable="False" BorderThickness="0,1,0,0" BorderBrush="{DynamicResource Theme.Border}" Margin="{DynamicResource Style.ProbeCardGrid.Margin}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Style="{DynamicResource Style.ProbeCard}"
                            MinWidth="150"
                            Background="{Binding Status, Converter={StaticResource ProbeStatusToBackgroundBrushConverter}}">
                        <Grid Name="gridItemsControl">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Ping output section -->
                            <DockPanel AllowDrop="True" Drop="Probe_Drop">

                                <!-- Ping title bar / button section -->
                                <Border DockPanel.Dock="Top"
                                        x:Name="headerBorder"
                                        CornerRadius="{DynamicResource Style.ProbeCardHeader.CornerRadius}"
                                        Cursor="SizeAll">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="{DynamicResource Theme.Surface}"/>
                                            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border}"/>
                                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ElementName=gridItemsControl, Path=IsMouseOver}" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource Theme.SurfaceAlt}"/>
                                                    <Setter Property="BorderBrush" Value="{DynamicResource Theme.Accent}"/>
                                                    <Setter Property="BorderThickness" Value="0,0,0,2"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" MinWidth="60"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Alias section -->
                                    <TextBlock Name="txtEditAlias"
                                           Text="{Binding Alias, UpdateSourceTrigger=PropertyChanged, NotifyOnTargetUpdated=True}"
                                           Background="Transparent"
                                           FontSize="13"
                                           VerticalAlignment="Center"
                                           Padding="{DynamicResource Style.ProbeCardAlias.Padding}"
                                           Margin="0"
                                           FontWeight="SemiBold"
                                           TextTrimming="CharacterEllipsis"
                                           Foreground="{Binding Status, Converter={StaticResource ProbeStatusToAliasBrushConverter}}"
                                           AllowDrop="True" Drop="Probe_Drop" PreviewMouseMove="ProbeTitle_PreviewMouseMove"/>

                                    <!-- Button: Remove probe -->
                                    <Button Grid.Column="3"
                                            Padding="6,4"
                                            Margin="0"
                                            Background="Transparent"
                                            HorizontalAlignment="Right"
                                            IsTabStop="False"
                                            ToolTip="{x:Static resource:Strings.Tooltip_Close}"
                                            ToolTipService.InitialShowDelay="1500"
                                            ToolTipService.ShowDuration="2000"
                                            Cursor="Arrow"
                                            Click="RemoveProbe_Click">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border Name="controlButton_border"
                                                        BorderThickness="0"
                                                        BorderBrush="Transparent"
                                                        CornerRadius="{DynamicResource Style.CornerRadius.Small}"
                                                        Padding="{TemplateBinding Padding}"
                                                        Background="{TemplateBinding Background}">
                                                    <Image Name="controlButton_image" Source="{StaticResource icon.window-close-black}"
                                                           Width="14" Height="14"
                                                           Opacity="0.6"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="controlButton_border" Property="Background" Value="{DynamicResource Theme.Danger}"/>
                                                        <Setter TargetName="controlButton_image" Property="Opacity" Value="1"/>
                                                        <Setter TargetName="controlButton_image" Property="Source" Value="{StaticResource icon.window-close-white}"/>
                                                    </Trigger>
                                                    <Trigger Property="IsPressed" Value="True">
                                                        <Setter TargetName="controlButton_border" Property="Background" Value="{DynamicResource Theme.Danger}"/>
                                                        <Setter TargetName="controlButton_border" Property="Opacity" Value="0.7"/>
                                                        <Setter TargetName="controlButton_image" Property="Opacity" Value="0.8"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                        <Button.Style>
                                            <Style TargetType="{x:Type Button}">
                                                <Setter Property="Opacity" Value="0.45"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ElementName=gridItemsControl, Path=IsMouseOver}" Value="True">
                                                        <Setter Property="Opacity" Value="1"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>

                                    <!-- Button: Isolated view -->
                                    <Button Grid.Column="2"
                                            Padding="6,4"
                                            Margin="0,0,1,0"
                                            Background="Transparent"
                                            HorizontalAlignment="Right"
                                            IsTabStop="False"
                                            ToolTip="{x:Static resource:Strings.Tooltip_IsolatedView}"
                                            ToolTipService.InitialShowDelay="1500"
                                            ToolTipService.ShowDuration="2000"
                                            Cursor="Arrow"
                                            Click="IsolatedView_Click">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border Name="controlButton_border"
                                                        BorderThickness="0"
                                                        BorderBrush="Transparent"
                                                        CornerRadius="{DynamicResource Style.CornerRadius.Small}"
                                                        Padding="{TemplateBinding Padding}"
                                                        Background="{TemplateBinding Background}">
                                                    <Image Name="controlButton_image" Source="{StaticResource icon.window-restore-black}"
                                                           Width="14" Height="14"
                                                           Opacity="0.6"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="controlButton_border" Property="Background" Value="{DynamicResource Theme.Accent}"/>
                                                        <Setter TargetName="controlButton_image" Property="Opacity" Value="1"/>
                                                        <Setter TargetName="controlButton_image" Property="Source" Value="{StaticResource icon.window-restore-white}"/>
                                                    </Trigger>
                                                    <Trigger Property="IsPressed" Value="True">
                                                        <Setter TargetName="controlButton_border" Property="Background" Value="{DynamicResource Theme.AccentHover}"/>
                                                        <Setter TargetName="controlButton_image" Property="Opacity" Value="0.5"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                        <Button.Style>
                                            <Style TargetType="{x:Type Button}">
                                                <Setter Property="Opacity" Value="0.45"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ElementName=gridItemsControl, Path=IsMouseOver}" Value="True">
                                                        <Setter Property="Opacity" Value="1"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>

                                    <!-- Button: Edit alias -->
                                    <Button Grid.Column="1"
                                            Padding="6,4"
                                            Margin="0,0,1,0"
                                            Background="Transparent"
                                            HorizontalAlignment="Right"
                                            IsTabStop="False"
                                            ToolTip="{x:Static resource:Strings.Tooltip_EditAlias}"
                                            ToolTipService.InitialShowDelay="1500"
                                            ToolTipService.ShowDuration="2000"
                                            IsEnabled="{Binding ElementName=Hostname, Path=Text, Converter={StaticResource StringLengthToBoolConverter}}"
                                            Cursor="Arrow"
                                            Click="EditAlias_Click">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border Name="controlButton_border"
                                                        BorderThickness="0"
                                                        BorderBrush="Transparent"
                                                        CornerRadius="{DynamicResource Style.CornerRadius.Small}"
                                                        Padding="{TemplateBinding Padding}"
                                                        Background="{TemplateBinding Background}">
                                                    <Image Name="controlButton_image" Source="{StaticResource icon.pencil-black}"
                                                           Width="14" Height="14"
                                                           Opacity="0.6"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="controlButton_border" Property="Background" Value="{DynamicResource Theme.Accent}"/>
                                                        <Setter TargetName="controlButton_image" Property="Opacity" Value="1"/>
                                                        <Setter TargetName="controlButton_image" Property="Source" Value="{StaticResource icon.pencil-white}"/>
                                                    </Trigger>
                                                    <Trigger Property="IsPressed" Value="True">
                                                        <Setter TargetName="controlButton_border" Property="Background" Value="{DynamicResource Theme.AccentHover}"/>
                                                        <Setter TargetName="controlButton_image" Property="Opacity" Value="0.5"/>
                                                    </Trigger>
                                                    <Trigger Property="IsEnabled" Value="False">
                                                        <Setter TargetName="controlButton_image" Property="Opacity" Value="0.4"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                        <Button.Style>
                                            <Style TargetType="{x:Type Button}">
                                                <Setter Property="Opacity" Value="0.45"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ElementName=gridItemsControl, Path=IsMouseOver}" Value="True">
                                                        <Setter Property="Opacity" Value="1"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                    </Grid>
                                </Border>

                                <!-- Ping statistics -->
                                <DockPanel DockPanel.Dock="Bottom"
                                           ClipToBounds="True"
                                           Style="{DynamicResource Style.ProbeCardStats}">
                                    <DockPanel.Resources>
                                        <Style TargetType="{x:Type TextBlock}">
                                            <Setter Property="DockPanel.Dock" Value="Left"/>
                                            <Setter Property="FontFamily" Value="Segoe UI"/>
                                            <Setter Property="FontSize" Value="11"/>
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                            <Setter Property="Padding" Value="{DynamicResource Style.ProbeCardStatsLabel.Padding}"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="Foreground" Value="{Binding Status, Converter={StaticResource ProbeStatusToStatisticsBrushConverter}}"/>
                                        </Style>
                                    </DockPanel.Resources>
                                    <TextBlock Text="Sent: " Opacity="0.65" />
                                    <TextBlock Text="{Binding Statistics.Sent, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, NotifyOnTargetUpdated=True}">
                                        <TextBlock.Triggers>
                                            <EventTrigger RoutedEvent="Binding.TargetUpdated">
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                         AutoReverse="True"
                                                                         From="1.0" To="0.75" Duration="0:0:0.3"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </EventTrigger>
                                        </TextBlock.Triggers>
                                    </TextBlock>
                                    <TextBlock Text="Received: " Opacity="0.65"
                                               Margin="{DynamicResource Style.ProbeCardStatsLabel.Margin}"/>
                                    <TextBlock Text="{Binding Statistics.Received, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, NotifyOnTargetUpdated=True}">
                                        <TextBlock.Triggers>
                                            <EventTrigger RoutedEvent="Binding.TargetUpdated">
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                         AutoReverse="True"
                                                                         From="1.0" To="0.75" Duration="0:0:0.3"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </EventTrigger>
                                        </TextBlock.Triggers>
                                    </TextBlock>
                                    <TextBlock Text="Lost: " Opacity="0.65"
                                               Margin="{DynamicResource Style.ProbeCardStatsLabel.Margin}"/>
                                    <TextBlock Text="{Binding Statistics.Lost, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, NotifyOnTargetUpdated=True}">
                                        <TextBlock.Triggers>
                                            <EventTrigger RoutedEvent="Binding.TargetUpdated">
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                         AutoReverse="True"
                                                                         From="1.0" To="0.75" Duration="0:0:0.3"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </EventTrigger>
                                        </TextBlock.Triggers>
                                    </TextBlock>
                                </DockPanel>

                                <!-- Ping history -->
                                <c:AutoScrollListBox x:Name="History"
                                                     BorderThickness="0"
                                                     Background="Transparent"
                                                     Padding="4,2,4,2"
                                                     Foreground="{Binding Status, Converter={StaticResource ProbeStatusToForegroundBrushConverter}}"
                                                     ItemsSource="{Binding History}"
                                                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                                     IsTabStop="False"
                                                     IsTextSearchEnabled="False"
                                                     FocusVisualStyle="{x:Null}"
                                                     FontFamily="Consolas"
                                                     FontSize="{Binding Type, Converter={StaticResource ProbeTypeToFontSizeConverter}}">
                                    <ListBox.ItemContainerStyle>
                                        <Style TargetType="{x:Type ListBoxItem}">
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                                        <ContentPresenter />
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Setter Property="IsTabStop" Value="False"/>
                                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                        </Style>
                                    </ListBox.ItemContainerStyle>
                                    <ListBox.Resources>
                                        <Style TargetType="{x:Type ScrollBar}" BasedOn="{StaticResource ScrollBarStyle.Probe}">
                                            <Setter Property="Opacity" Value="0.6"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ElementName=gridItemsControl, Path=IsMouseOver}" Value="True">
                                                    <Setter Property="Opacity" Value="1.0"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ListBox.Resources>
                                </c:AutoScrollListBox>
                            </DockPanel>


                            <!-- MultiPingMonitor icon -->
                            <Image Source="{StaticResource icon.vmping-logo-simple}"
                                   Width="72"
                                   Height="72"
                                   IsHitTestVisible="False"
                                   Visibility="{Binding IsActive, Converter={StaticResource InverseBooleanToVisibilityConverter}, NotifyOnTargetUpdated=True}"
                                   TargetUpdated="Logo_TargetUpdated"
                                   Opacity="0.03"/>

                            <!-- Hostname and ping button section -->
                            <Grid Grid.Row="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Border to set background color and top border for this section -->
                                <Border Grid.ColumnSpan="3" Style="{DynamicResource Style.ProbeCardFooter}"/>

                                <!-- Status glyph -->
                                <TextBlock Grid.Column="0"
                                           Text="{Binding Status, Converter={StaticResource ProbeStatusToGlyphConverter}}"
                                           FontSize="14"
                                           FontFamily="Marlett"
                                           VerticalAlignment="Center"
                                           Foreground="{DynamicResource Theme.Text.Primary}"
                                           Padding="{DynamicResource Style.ProbeCardGlyph.Padding}">
                                </TextBlock>

                                <!-- Hostname textbox -->
                                <TextBox Name="Hostname"
                                         Grid.Column="1"
                                         FontWeight="SemiBold"
                                         FontSize="{Binding ElementName=History, Path=ActualWidth, Converter={StaticResource HostnameFontsizeConverter}}"
                                         BorderThickness="0,0,0,0"
                                         BorderBrush="{DynamicResource Theme.Border}"
                                         VerticalContentAlignment="Center"
                                         Background="Transparent"
                                         Padding="{DynamicResource Style.ProbeCardHostname.Padding}"
                                         Text="{Binding Hostname, UpdateSourceTrigger=PropertyChanged}"
                                         IsEnabled="{Binding IsActive, Converter={StaticResource InverseBooleanConverter}}"
                                         Template="{StaticResource TextBoxControlTemplate}"
                                         Loaded="Hostname_Loaded"
                                         TextChanged="Hostname_TextChanged"
                                         KeyDown="Hostname_KeyDown"/>

                                <!-- Ping button -->
                                <Button Click="ProbeStartStop_Click"
                                        Grid.Column="2"
                                        FontSize="14"
                                        Padding="0,2"
                                        Margin="2"
                                        HorizontalAlignment="Right"
                                        Background="{DynamicResource Theme.Surface}">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Name="pingButton_border" BorderThickness="1" BorderBrush="{DynamicResource Theme.Accent}"
                                                    CornerRadius="{DynamicResource Style.CornerRadius.Button}"
                                                    Background="{TemplateBinding Background}">
                                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10,0">
                                                    <Image Source="{Binding IsActive, Converter={StaticResource BooleanToImageConverter}}" Width="16" Height="16"
                                                           Margin="0,6"/>
                                                    <TextBlock Text="{Binding IsActive, Converter={StaticResource BoolToStringConverter}}"
                                                               Visibility="{Binding ElementName=History, Path=ActualWidth, Converter={StaticResource ButtonTextVisibilityConverter}}"
                                                               Foreground="{DynamicResource Theme.Text.Primary}"
                                                               FontSize="13"
                                                               VerticalAlignment="Center"
                                                               Margin="8,0,0,0"/>
                                                </StackPanel>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="pingButton_border" Property="Background" Value="{DynamicResource Theme.SurfaceAlt}"/>
                                                    <Setter TargetName="pingButton_border" Property="BorderBrush" Value="{DynamicResource Theme.AccentHover}"/>
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter TargetName="pingButton_border" Property="Background" Value="{DynamicResource Theme.SurfaceAlt}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>

                            </Grid>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>

            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <!-- ColumnCount.Tag is updated to contain the lesser the two values: ColumnCount.Value and _ProbeCollection.Count -->
                    <UniformGrid Columns="{Binding ElementName=ColumnCount, Path=Tag}"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
    </DockPanel>
    </Border>
</Window>
