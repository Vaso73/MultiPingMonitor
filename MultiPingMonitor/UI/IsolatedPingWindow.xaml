<Window x:Class="MultiPingMonitor.UI.IsolatedPingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Hostname, StringFormat='{}{0} - MultiPingMonitor'}"
        UseLayoutRounding="True"
        WindowStyle="None"
        Height="500"
        Width="450"
        MinHeight="200"
        MinWidth="300"
        Background="{DynamicResource Theme.Background}"
        Closed="Window_Closed">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32"
                      ResizeBorderThickness="{x:Static SystemParameters.WindowResizeBorderThickness}"
                      GlassFrameThickness="0"
                      CornerRadius="0" />
    </WindowChrome.WindowChrome>

    <Border Style="{DynamicResource Style.WindowMainPanel}">
        <DockPanel Background="{DynamicResource Theme.Background}">

            <!-- Title bar -->
            <Grid DockPanel.Dock="Top" Height="32" Background="{DynamicResource Theme.Surface}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <!-- Window icon -->
                <Image Grid.Column="0" Source="/MultiPingMonitor.ico" Width="16" Height="16" Margin="8,0,4,0"
                       RenderOptions.BitmapScalingMode="HighQuality" />
                <!-- Title text (hostname) -->
                <TextBlock Grid.Column="1" Text="{Binding Hostname}"
                           VerticalAlignment="Center" FontSize="12"
                           Foreground="{DynamicResource Theme.Text.Secondary}" Margin="4,0,0,0"
                           TextTrimming="CharacterEllipsis" />
                <!-- Minimize -->
                <Button Grid.Column="2" Style="{DynamicResource Style.TitleBarButton}"
                        Width="46" Click="MinimizeButton_Click"
                        WindowChrome.IsHitTestVisibleInChrome="True">
                    <Path Width="10" Height="1" Data="M0,0 H10" Stretch="None"
                          Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                          StrokeThickness="1" />
                </Button>
                <!-- Close -->
                <Button Grid.Column="3" Style="{DynamicResource Style.TitleBarCloseButton}"
                        Width="46" Click="CloseButton_Click"
                        WindowChrome.IsHitTestVisibleInChrome="True">
                    <Path Width="10" Height="10" Data="M0,0 L10,10 M0,10 L10,0" Stretch="None"
                          Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                          StrokeThickness="1" />
                </Button>
            </Grid>

            <!-- Hostname header -->
            <Border DockPanel.Dock="Top" Style="{DynamicResource Style.HeaderPanel}">
                <TextBlock Text="{Binding Hostname}"
                           FontSize="16" FontWeight="SemiBold"
                           Foreground="{DynamicResource Theme.Text.Primary}"
                           TextTrimming="CharacterEllipsis" />
            </Border>

            <!-- Stats footer (Ping only, hidden when Sent == 0) -->
            <Border DockPanel.Dock="Bottom">
                <Border.Style>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Path=Type}" Value="Ping">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Path=Statistics.Sent}" Value="0">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Border Style="{DynamicResource Style.FooterPanel}">
                <DockPanel>
                    <DockPanel.Resources>
                        <Style TargetType="{x:Type TextBlock}">
                            <Setter Property="DockPanel.Dock" Value="Left"/>
                            <Setter Property="FontFamily" Value="Segoe UI"/>
                            <Setter Property="FontSize" Value="12"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Foreground" Value="{Binding Status, Converter={StaticResource ProbeStatusToStatisticsBrushConverter}}"/>
                        </Style>
                    </DockPanel.Resources>
                    <TextBlock Text="Sent: " Opacity="0.8" Padding="6,0,0,3" />
                    <TextBlock Text="{Binding Statistics.Sent, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, NotifyOnTargetUpdated=True}">
                        <TextBlock.Triggers>
                            <EventTrigger RoutedEvent="Binding.TargetUpdated">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                         AutoReverse="True"
                                                         From="1.0" To="0.75" Duration="0:0:0.3"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </TextBlock.Triggers>
                    </TextBlock>
                    <TextBlock Text="  Received: " Opacity="0.8"/>
                    <TextBlock Text="{Binding Statistics.Received, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, NotifyOnTargetUpdated=True}">
                        <TextBlock.Triggers>
                            <EventTrigger RoutedEvent="Binding.TargetUpdated">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                         AutoReverse="True"
                                                         From="1.0" To="0.75" Duration="0:0:0.3"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </TextBlock.Triggers>
                    </TextBlock>
                    <TextBlock Text="  Lost: " Opacity="0.8"/>
                    <TextBlock Text="{Binding Statistics.Lost, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, NotifyOnTargetUpdated=True}">
                        <TextBlock.Triggers>
                            <EventTrigger RoutedEvent="Binding.TargetUpdated">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                         AutoReverse="True"
                                                         From="1.0" To="0.75" Duration="0:0:0.3"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </TextBlock.Triggers>
                    </TextBlock>
                </DockPanel>
                </Border>
            </Border>

            <!-- Ping history (status-tinted background) -->
            <Border Background="{Binding Status, Converter={StaticResource ProbeStatusToBackgroundBrushConverter}}"
                    BorderBrush="{DynamicResource Theme.Border}" BorderThickness="0,1,0,0">
                <TextBox x:Name="History"
                         IsReadOnly="True"
                         IsTabStop="False"
                         Background="Transparent"
                         Foreground="{Binding Status, Converter={StaticResource ProbeStatusToForegroundBrushConverter}}"
                         HorizontalScrollBarVisibility="Disabled"
                         VerticalScrollBarVisibility="Auto"
                         VerticalContentAlignment="Top"
                         BorderThickness="0"
                         Cursor="Arrow"
                         IsInactiveSelectionHighlightEnabled="True"
                         SelectionBrush="Black"
                         SelectionOpacity="0.4"
                         TextChanged="History_TextChanged"
                         SelectionChanged="History_SelectionChanged"
                         FontFamily="Consolas"
                         FontSize="{Binding Type, Converter={StaticResource ProbeTypeToFontSizeConverter}}"
                         Text="{Binding Path=HistoryAsString, Mode=OneWay}">
                    <TextBox.Resources>
                        <Style TargetType="{x:Type ScrollBar}">
                            <Setter Property="Opacity" Value="0.4"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Path=IsMouseOver, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                                    <Setter Property="Opacity" Value="1.0"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Resources>
                </TextBox>
            </Border>

        </DockPanel>
    </Border>
</Window>
